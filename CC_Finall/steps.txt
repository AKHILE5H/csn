GAE ERROR:

# refreshing the repositories
sudo apt update

sudo apt upgrade
# installing python 2.7 and pip for it

sudo apt install python2.7 python-pip
# installing python-pip for 3.6

sudo apt install python3-pip

RedHat: more etc/os-release
UB:lsb_release -a

sudo apt install openssh-server
sudo apt install net-tools
scp new.txt new2.txt vuser@10.10.2.4:
ssh vuser@10.10.2.4 “ls”

Angular:

2 terminals: First cd into user app for both

1 for npm i and npm start

1 for angular commands: 

	npm i -g json-server
	json-server --watch db.json

App manager->new lightning app->create app
Lightning app builder->app page
ojb manageer->new custom obj->fields and relat->new->add fields
To show obj you need custom tab->home->search tabs->select object

history -c && history -w

To start sudo
1. su
2. Enter pass
3. apt install sudo
4. usermod -aG sudo user-name
5. exit (command exits root dir)
6. reboot (command reboots the system)
